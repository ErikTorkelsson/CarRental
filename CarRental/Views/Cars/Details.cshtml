@model CarRental.Models.Car

@{
    ViewData["Title"] = "Details";
}

<div class="container">

    <h1>@Model.Brand @Model.Model</h1>
    <img src="@Model.ImgSrc" />
    <table class="table">
        <thead>

        </thead>
        <tbody>
            <tr>
                <td>Brand</td>
                <td>@Model.Brand</td>
            </tr>
            <tr>
                <td>Model</td>
                <td>@Model.Model</td>
            </tr>
            <tr>
                <td>Year</td>
                <td>@Model.Year</td>
            </tr>
            <tr>
                <td>Seats</td>
                <td>@Model.Seats</td>
            </tr>
            <tr>
                <td>Mileage</td>
                <td>@Model.Mileage</td>
            </tr>
            <tr>
                <td>Type</td>
                <td>@Model.combisudan</td>
            </tr>
            <tr>
                <td>Fuel</td>
                <td>@Model.Fuel</td>
            </tr>
            <tr>
                <td>
                    <a asp-controller="Rentals" asp-action="Create" class="btn btn-primary">Rent</a>
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-primary">Delete</a>
                </td>
                <td></td>
            </tr>
            <tr>
                <td></td>
                <td></td>
            </tr>


        </tbody>
    </table>
</div>


<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
